%h5= @category.name

%ul#tools_list
  - @category.tools.each do |tool|
    %li
      #{link_to tool.name, admin_tool_path(current_user, tool)}: #{link_to tool.user.username, admin_user_path(current_user, tool.user)}
